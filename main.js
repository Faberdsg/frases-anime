const lista = [1, 2, 3, 4, 5, 6, 7, 8];

console.log(Math.floor(Math.random) * lista.length);

const indexRandom = Math.floor(Math.random() * lista.length);

console.log(indexRandom);

console.log(lista[indexRandom]);
